package com.example.euro24application.data
/*
import androidx.lifecycle.ViewModel
import androidx.navigation.NavController
import androidx.navigation.compose.rememberNavController
import com.example.euro24application.screens.Screen

class RegisterViewModel(private val userDao: UserDao, val navController: NavController) : ViewModel() {

      fun insert(name: String, email: String, password: String) {
        if (name.isNotEmpty() && email.isNotEmpty() && password.isNotEmpty()) {
            userDao.insert(
                User(
                    fullName = name,
                    email = email,
                    password = password
                )
            )
        }
          else{
              navController.navigate(Screen.RegisterScreen.route)
        }
    }
}*/